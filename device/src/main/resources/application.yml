server:
  tomcat:
    uri-encoding: UTF-8
  port: 8082
  servlet:
    context-path: /
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://10.8.108.15:3306/robot_restaurant?useUnicode=true&characterEncoding=utf8&serverTimezone=GMT%2B8&useSSL=false
    username: root
    password: Bgy@123456
    druid:
      initial-size: 1
      min-idle: 1
      max-actice: 20
      test-on-borrow: true
      stat-view-servlet:
        allow: true
  thymeleaf:
    prefix: classpath:/templates/
    suffix: .html
    mode: LEGACYHTML5
    encoding: UTF-8
    servlet:
      content-type: text/html
    cache: false
  mvc:
    static-path-pattern: /static/**
  redis:
    database: 1
    host: 10.8.108.15
    port: 6379
    jedis:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0
    timeout: 0ms

restart:
  include:
    mapper: /mapper-[\\w-\\.]+jar
    pagehelper: /pagehelper-[\\w-\\.]+jar

mybatis:
  type-aliases-package: com.bgy.device.entity
  mapper-locations: classpath:mapper/*.xml

mapper:
  mappers: com.bgy.device.utils.MyMapper
  not-empty: false
  identity: MYSQL
pagehelper:
  helperDialect: mysql
  reasonable: true
  supportMethodsArguments: true
  params: count=countSql